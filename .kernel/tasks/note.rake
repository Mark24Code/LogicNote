namespace "note" do
  desc "note: create note."
  task :create do
    puts "create"
    # Build the main program
  end

  desc "note:search note."
  task :search do
    puts "search"
    # Build the main program
  end

  desc "note:delete note."
  task :delete do
    puts "delete"
    # Build the main program
  end
end